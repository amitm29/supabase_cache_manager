name: flutter_cache_manager_supabase
description: "CacheManager implementation for supabase storage. Inspired from flutter_cache_manager_firebase"
version: 0.0.1
homepage: https://github.com/amitm29/supabase_cache_manager

environment:
  sdk: '>=3.4.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.1
  retry: ^3.1.2
  supabase_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0